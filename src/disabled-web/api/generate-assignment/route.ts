import { View, Text } from 'react-native';

export default function GenerateAssignmentRoutePlaceholder() {
  return (
    <View>
      <Text>API route is not available in Expo Go runtime.</Text>
    </View>
  );
}
